rootProject.name = 'com.lessspring.org'
include 'storage'
include 'config-server'
include 'api'
include 'client'
include 'common'

enableFeaturePreview("GRADLE_METADATA")

rootProject.children.each {project ->
    project.buildFileName = "build.gradle"
}
include 'test'

